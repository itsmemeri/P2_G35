#include <istream>
#include <ostream>
#include <iostream>
#include <fstream>
#include <unordered_map>
#include <vector>
#include <string>
#include <utility>
#include <algorithm>
#include <Windows.h>
#pragma once

struct pair_hash;

void Combinaciones();
void Print();
void Combinar(int pos1, int pos2);
void Add(int pos);
void Addbasics();
void Delete(int pos);
void Info(int pos);
void Sort();
void Clean();
